import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test

class AlphabetWarKtTest{
    @Test
    fun `Should Right side wins! when right side stronger than left side`() {
        val expected =""
        assertEquals("Right side wins!", expected)
    }

}